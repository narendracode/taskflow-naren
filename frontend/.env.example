# Base URL of the backend API — must be reachable from the browser.
# Local dev: backend runs on port 8000
# Docker:    override this build arg to match your exposed API port
VITE_API_URL=http://localhost:8000
